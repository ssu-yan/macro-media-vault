# 事故紀錄：2026-08-25 協議被舊草稿覆蓋

**發生時間**：2026-08-25，Lucas 在對話中處理「補地緣衝突盲區」的請求時
**影響對象**：`claude/G3-每週掃描協議.md`（當時為 v3.2，2026-08-21）
**處置**：已從搜尋索引重建並寫回，**待 Wendy 核對**

---

## 發生了什麼

1. Wendy 提到 G3 首次掃描的結果（12 候選／4 合格）。
2. Lucas 讀了 `claude/G3-掃描-2026-08-14.md`，做了審計，
   發現「四筆登記的入口節點都是既有節點、輸出不含當週事件資訊」這個問題。
   **這個審計本身是對的**，而且與後來 08-15 的正式審計結論一致。
3. Lucas 接著 `project_read` 讀了協議，看到的是 **v2**，
   於是寫了一份 v3 草稿並 `project_write` 覆蓋。
4. **但當時協議的實際版本是 v3.2**（2026-08-21），
   已經歷 v3 → v3.1 → v3.2 三輪修訂，內容比 Lucas 那份草稿完整得多。
5. 結果：v3.2 被一份不知情的草稿整份取代。

## 為什麼會發生

**直接原因：`project_read` 回傳的內容與 Lucas 的判斷之間有落差。**
Lucas 讀到的內容是 v2，但實際檔案已是 v3.2。無論是索引延遲、快取、
還是讀取當下的狀態如此，**Lucas 都沒有在寫入前做第二次確認**。

**根本原因：Lucas 在對一份「自己不知道中間發生過什麼事」的文件做整份覆蓋。**

這次對話從 08-14 的脈絡直接接到 08-25，中間 08-15 到 08-24 的
四份掃描紀錄、一份登記處置、一份補圖提案、一份補圖決策、
以及協議的三輪修訂，Lucas **完全不知情**。
在這種狀態下寫出一份「v3」並覆蓋現行版本，是明顯的越權。

**可以攔下來的三個時點，全部錯過：**

| 時點 | 本來可以怎麼做 | 實際 |
|---|---|---|
| 對話開始 | 系統提示已列出 17 份 Project 文件，含 `G3-掃描-2026-08-24.md` 等明顯是後續進度的檔名 | 沒有先讀 |
| 讀協議時 | 看到 v2 但檔案列表有 08-18／08-20 的文件，時序對不上 | 沒有察覺 |
| 寫入前 | 對已存在且他人維護中的文件，做增量修訂而非整份取代 | 直接整份覆蓋 |

## 損害與復原

**復原方式**：`project_search` 的索引尚未重建，仍持有 v3.2 的內容。
用四次針對性查詢把各章節撈回，重建後寫回。

**已還原**：標頭與版本宣告、〈v3 改變了這個協議是什麼〉、〈為什麼存在〉、
〈圖的真實來源是 seed 腳本〉、第 1–8 步全部、計分規則與獨立性、
〈讀引擎輸出〉、〈已知未解的問題〉1–8、變更紀錄四段（v1→v2→v3→v3.1→v3.2）。

**不確定的部分**：重建自搜尋片段，**可能有微幅字句差異**。
規則內容經逐條比對應無缺漏，但**這是重建版，不是原件**。

**沒有受影響的**：所有已凍結的掃描紀錄、審計、登記處置、補圖提案與決策，
以及 vault 的圖與引擎。本次只覆蓋了協議一份檔案。

## 已納入協議的防護

協議〈已知未解的問題〉新增第 9 條：

> **本協議本身沒有版本保護。** 任何 session 都可以用 `project_write` 整份覆蓋它，
> 不會有衝突偵測、不會有備份。
> **緩解**：改本檔前必須先 `project_read` 確認現行版本號，
> 且只以「在現行版本上做增量修訂」的方式改寫，不得用外部草稿整份取代。
> **未緩解**：沒有機制強制執行這條，只能靠紀律。

## 給 Wendy 的三件事

1. **請核對重建版**，特別是第 1 步的寫死數字（81／140／84）與第 8 節的補圖流程。
   任何一條與你記憶不符，以你的記憶為準。
2. **考慮把協議也放進 vault 並納入 Git。** 目前它只存在於 Project，
   而 Project 沒有版本歷史。放進 vault 後，Git 就是它的備份——
   這次的復原靠的是搜尋索引還沒重建，那是運氣，不是機制。
3. **本次的審計結論不受影響。** 它與 08-15 的正式審計獨立得出相同結論
   （四筆登記無效、資格條件寫錯），只是那件事早在十天前就處理完了。

## 一句話總結

**做對的分析，用錯的方式落地。** 審計本身沒有問題，
問題出在沒有先確認自己看到的世界是不是最新的就動手覆蓋。
